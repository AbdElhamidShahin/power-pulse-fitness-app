<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.powerteam.powerpulse">
    <!--  ─── Existing permissions ────────────────────────────  -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!--  ─── FIX: Required by flutter_local_notifications v20+ ─
             POST_NOTIFICATIONS  : Android 13+ (API 33+) runtime notification permission.
             VIBRATE             : Allows notifications to vibrate the device.
             RECEIVE_BOOT_COMPLETED: Lets the plugin re-schedule notifications after
                                   device reboot via ScheduledNotificationBootReceiver.
             SCHEDULE_EXACT_ALARM: Required for AndroidScheduleMode.exactAllowWhileIdle
                                   on Android 12+ (API 31+). Without this, zonedSchedule
                                   with exactAllowWhileIdle will throw a SecurityException.
        ────────────────────────────────────────────────────────  -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!--  Required by the pedometer package on Android 10+ (API 29+).
             Without this the step-sensor stream delivers no events on modern devices.
             Runtime permission request is handled in PedometerCubit via the
             pedometer package's own permission check flow.  -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <application android:label="Power Pulse " android:name="${applicationName}" android:icon="@mipmap/launcher_icon">
        <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop" android:theme="@style/LaunchTheme" android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode" android:hardwareAccelerated="true" android:windowSoftInputMode="adjustResize">
            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--  FIX: ScheduledNotificationReceiver
                     Fires the scheduled notification at the exact time specified by
                     zonedSchedule(). Without this receiver, no scheduled notification
                     will ever appear — the alarm fires but nothing handles it.
                 -->
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" android:exported="false"/>
        <!--  FIX: ScheduledNotificationBootReceiver
                     Listens for ACTION_BOOT_COMPLETED and ACTION_MY_PACKAGE_REPLACED
                     so all pending scheduled notifications are re-registered after the
                     device reboots or the app is updated (pending alarms are cleared on
                     reboot by Android). Requires RECEIVE_BOOT_COMPLETED permission above.
                 -->
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!--  Don't delete the meta-data below.
                     This is used by the Flutter tool to generate GeneratedPluginRegistrant.java  -->
        <meta-data android:name="flutterEmbedding" android:value="2"/>
    </application>
    <!--  Required to query activities that can process text, see:
             https://developer.android.com/training/package-visibility?hl=en and
             https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

             In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin.  -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>